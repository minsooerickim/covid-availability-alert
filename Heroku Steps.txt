1. push to github
2. make app on heroku and use github method to deploy
3. get required files
	- requirements.txt (pip freeze > requirements.txt)
	- runtime.txt (manually add with what python version e.g python-3.9.2)
	- Procfile (worker if no user inputs or nothing like a bot, if not then web)

*view logs (heroku logs --app appname)

deploying selenium
https://github.com/akjasim/cb_selenium-python-deployment-heroku